import React from 'react';
import ReactDOM from 'react-dom/client';
import CigarCollectionApp from './CigarCollectionApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CigarCollectionApp />
  </React.StrictMode>
);
